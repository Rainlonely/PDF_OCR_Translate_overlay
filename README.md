# ForStella

This repository keeps the PDF processing scripts and the minimal directory
layout used by those scripts.

Tracked:
- `scripts/`
- directory placeholders under `output/pdf/` and `tmp/pdfs/`

Ignored:
- generated outputs
- temporary OCR assets
- local operating system metadata
